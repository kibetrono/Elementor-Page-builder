var elem = document.querySelectorAll('#swske-share');
for (var i = 0; i < elem.length; i++) {
    var currentEl = elem[i];
    // currentEl.style.color = 'red';
    currentEl.setAttribute("class", "btn btn-secondary btn-md");
}	